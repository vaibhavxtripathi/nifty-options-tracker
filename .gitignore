# Secrets — never commit
.env
*.env
!.env.example

# Phase 0 throwaway: live broker session tokens. Never commit.
tool/.session.json

# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Firebase config (contains project identifiers)
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# --- Flutter scaffold additions (Phase 1) ---
# Miscellaneous
*.class
*.log
*.swp
.atom/
.build/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ
*.ipr
*.iws

# Flutter/Dart/Pub
**/doc/api/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android build artifacts
/android/app/debug
/android/app/profile
/android/app/release
.widget_preview/

# FlutterFire: generated, but regenerable and not a secret store.
# google-services.json stays ignored above.

# Local Claude Code settings — tooling, not app config
.claude/
